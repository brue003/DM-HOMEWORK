# 집합

## 코딩에서의 집합
set(), 중복이 없으며 순서가 없어서 인덱싱이 불가능함

**교집합** / & / intersection / 논리 AND

**합집합** / | / union / 논리 OR

**차집합** / - / difference / 논리 AND NOT

**대칭차집합** / ^ / symmetric_difference 논리 XOR

s={1,2,3}

add(4) / 한 개 추가 / {1,2,3,4}

update([5,6,7) / 여러 개 추가 / {1,2,3,4,5,6,7}

remove(1) / 특정 원소 삭제 / {2,3,4,5,6,7}

discard(1) / 에러 없는 삭제 / {2,3,4,5,6,7}

pop() / 아무거나 삭제 ex) / {3,4,5,6,7}

clear() / 모든 원소를 지움 ( 빈 집합으로 만듦 ) / { }

isdisjoint() / 서로소이면 True

issubset(n) / n의 부분집합이면 True

issuperset(n) / n이 부분집합이면 True

in n / n이 있는지 확인

## 문제 풀이










