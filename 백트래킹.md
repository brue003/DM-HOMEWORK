# 완전탐색

모든 경우의 수를 완전히 탐색하는 방법이다.

**장점**은 방법을 확실히 찾을 수 있다는 것이고 **단점**은 시간이 매우 오래 걸린다는 것이다. 

# 백트래킹

백트래킹은 가능성 없는 루트는 가지 않고 되돌아가며 경우의 수를 해보는 알고리즘이다.

백트래킹의 장점은 경우의 수의 모든 부분을 보는 게 아니라 가능성이 없다고 판단되는 부분은 가다가 되돌아간다.

백트래킹의 특징

1. 가지치기 - 가능성이 없으면 되돌아간다. 이 모습이 가지치기 같다.
2. 재귀 - 함수를 다시 반복한다.

백트래킹의 과정

1. **전진**(시도한다)
2. **검사**(가능성을 본다.)
3. **가지치기**(가능성에 따라 행동한다.)
4. **복귀**(다시 되돌아간다. 여기서 중요한 점은 전진을 원상태로 되돌려 놓는 것이다.)
5. **재탐색**(다른 길을 찾는다.)

 # 순열과 조합

**순열** : 서로 다른 n개 중에서 r개를 선택해 순서를 따져서 나열하는 것이다.

**조합** : 서로 다른 n개 중에서 r개를 선택해 순서를 따지지 않고 나열하는 것이다.

예를 들면 순열은 (1,3) 과 (3,1)을 다르게 취급한다. 반면 조합은 (1,3) 과 (3,1) 같은 걸로 생각해서 하나만 나열한다.

```from itertools import permutations(combinations)```로 쓸 수 있다.


---문제 넣을 자리---

















