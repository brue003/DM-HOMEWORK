# 나머지연산 및 주기성

**나머지연산**

나머지연산은 나누기로 나온 몫을 제외한 **나머지를 구하는 연산**이다.

%이나 mod가 있다.

나머지연산의 특징은 **반복된다는 것**이다.

만약 어떤 A를 N으로 나누면 나올 수 있는 나머지는 **0~(N-1)** 이다.

만약 A가 0에서부터 1씩 계속 커질 때, N을 3이라 한다면 나머지는 0 -> 1 -> 2 -> 0 으로 무한히 반복한다.

이는 **주기성**을 띈다.

주기성이 쓰이는 곳은 다양하다.

요일 계산, 시간 계산이나 홀수짝수 구별에도 쓰인다.

**정리: 나머지연산은 수들의 범위를 제한한다. 이는 주기성을 띈다.**

### 문제

<img width="943" height="669" alt="KakaoTalk_20260905_152616159" src="https://github.com/user-attachments/assets/bcefb20a-06a3-44c2-b8e0-68e46da35bc4" />

```
import sys
import math
input = sys.stdin.readline

print("테스트 케이스의 개수를 입력하시오:")
T=int(input())


for i in range(T):
    print("정수 a,b를 입력하시오.(a는 밑, b는 지수이다.)")
    a, b =map(int, input().split())
    if (a**b)%10==0:
        print(10)
    else:
        print(((a**b))%10)
```
정확한 답:
```
import sys
import math
input = sys.stdin.readline

print("테스트 케이스의 개수를 입력하시오:")
T=int(input())

for _ in range(T):
    print("정수 a,b를 입력하시오.(a는 밑, b는 지수이다.)")
    a, b = map(int, input().split())

    result=pow(a,b,10)

    if result==0:
        print(10)
    else:
        print(result)
```
## 요일 계산기

```
# Day Of The Week Calculator

print("오늘 요일을 쓰시오: ")
D=input()

week={0:"월요일",1:"화요일",2:"수요일",3:"목요일",4:"금요일",5:"토요일",6:"일요일"}

print("본 계산기는 며칠 뒤의 요일을 계산합니다. 며칠 뒤가 궁금하신가요?(숫자로 작성하시오) : ")
F=int(input())

D,=[key for key, value in week.items() if value==D]

print(week.get((D+F)%7))
```








